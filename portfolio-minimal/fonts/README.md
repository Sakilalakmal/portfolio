# Canela.light font

Place your font file here:

- `portfolio-minimal/fonts/canela-light.woff2`

This portfolio's `styles.css` defines `@font-face` for `font-family: "Canela.light"` and will load the file above if present.
